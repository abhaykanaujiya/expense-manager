import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    backgroundImage: "   repeating-radial-gradient( circle at 0 0, #eee, #ccc 50px )",
     borderRadius: "15px",
  },
  cartContent: {
    paddingTop: 0,
  },
  divider: {
    margin: "20px 0",
  },
  title: {
    textAlign: "center",
  },
}));
